\VAR{ comp.commercial }

\textbf{Rôle~:} \VAR{ comp.role }


\textbf{Paquets principaux~:} \BLOCK{ for p in comp.packages_list }\texttt{\VAR{ p }}\BLOCK{ if not loop.last }, \BLOCK{ endif }\BLOCK{ endfor }


\textbf{Ports par défaut~:} \VAR{ comp.default_ports }

\BLOCK{ if comp.nodes }
\textbf{Nœud(s) hébergeant ce composant chez \VAR{ client.name }~:}
\begin{longtable}{|p{3.2cm}|p{4.5cm}|p{3.2cm}|p{2.3cm}|}
\hline
\rowcolor{primary}\tblhead{Nœud} & \tblhead{Hostname} & \tblhead{IP} & \tblhead{Zone} \\
\hline
\endhead
\BLOCK{ for n in comp.nodes }
\VAR{ n.id } & \VAR{ n.hostname } & \VAR{ n.ip } & \VAR{ n.zone_label } \\
\hline
\BLOCK{ endfor }
\end{longtable}
\BLOCK{ else }
\placeholder{Aucun nœud n'est explicitement associé à ce composant --- à compléter}
\BLOCK{ endif }

\textbf{Points d'attention exploitation~:}
\begin{itemize}[nosep]
  \item Un seul nœud Master doit exister à un instant donné --- une double-écriture (« split-brain ») corromprait l'annuaire.
\BLOCK{ if ldap_replica_nodes }
  \item Ce Master réplique vers~: \BLOCK{ for n in ldap_replica_nodes }\VAR{ n.id } (\VAR{ n.hostname })\BLOCK{ if not loop.last }, \BLOCK{ endif }\BLOCK{ endfor }.
\BLOCK{ else }
  \item \placeholder{Aucun Directory Replica configuré --- absence de haute disponibilité LDAP à documenter/valider avec le client}
\BLOCK{ endif }
  \item En cas de panne prolongée du Master, une procédure de promotion manuelle d'un Replica en Master doit être documentée et testée (voir le PCA/PRA, \S\ref{sec:pca-pra}).
\end{itemize}
