\VAR{ comp.commercial }

\textbf{Rôle~:} \VAR{ comp.role }


\textbf{Paquets principaux~:} \BLOCK{ for p in comp.packages_list }\texttt{\VAR{ p }}\BLOCK{ if not loop.last }, \BLOCK{ endif }\BLOCK{ endfor }


\textbf{Ports par défaut~:} \VAR{ comp.default_ports }

\BLOCK{ if comp.nodes }
\textbf{Nœud(s) hébergeant ce composant chez \VAR{ client.name }~:}
\begin{longtable}{|p{3.2cm}|p{4.5cm}|p{3.2cm}|p{2.3cm}|}
\hline
\rowcolor{primary}\tblhead{Nœud} & \tblhead{Hostname} & \tblhead{IP} & \tblhead{Zone} \\
\hline
\endhead
\BLOCK{ for n in comp.nodes }
\VAR{ n.id } & \VAR{ n.hostname } & \VAR{ n.ip } & \VAR{ n.zone_label } \\
\hline
\BLOCK{ endfor }
\end{longtable}
\BLOCK{ else }
\placeholder{Aucun nœud n'est explicitement associé à ce composant --- à compléter}
\BLOCK{ endif }

\textbf{Points d'attention exploitation~:}
\begin{itemize}[nosep]
  \item Ces nœuds sont en lecture seule : toute tentative d'écriture doit être dirigée vers le Directory Master.
\BLOCK{ if ldap_master_nodes }
  \item Synchronisés depuis~: \BLOCK{ for n in ldap_master_nodes }\VAR{ n.id } (\VAR{ n.hostname })\BLOCK{ if not loop.last }, \BLOCK{ endif }\BLOCK{ endfor }.
\BLOCK{ else }
  \item \placeholder{Aucun Directory Master identifié dans la configuration --- à corriger}
\BLOCK{ endif }
  \item Utiles pour répartir la charge des requêtes d'authentification et assurer une continuité de service en cas d'indisponibilité temporaire du Master (lecture seule).
\end{itemize}
